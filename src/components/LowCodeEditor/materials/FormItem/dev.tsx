/**
 * 表单项组件（开发态）
 *
 * 在编辑器中作为表单项组件，用于被放置到Form组件中
 * 实际上是一个空组件，因为在Form中会根据FormItem的配置信息渲染实际的表单项
 * 主要用于低代码编辑器中的布局和拖拽占位
 *
 * @returns {JSX.Element} 空的Fragment组件
 */
const FormItem = () => <></>;

export default FormItem;
